<section class="section">
  <div class="container">
    <div class="columns">
      <% if logged_in? %>
        <%= render 'profile' %>
      <% else %>
        <%= render 'trends' %>
      <% end %>
      <%= render 'feed' %>
      <%= render 'who-to-follow' %>
    </div>
  </div>
</section>
<%= link_to 'New Opinion', new_opinion_path %>
